# Spec-Driven Development

- For new product work, prefer the SDD path: requirements grilling, PRD, issue decomposition, then TDD implementation.
- Keep HITL checkpoints explicit when moving between specification stages.
- For focused bug fixes or small code changes, implement directly with tests sized to the risk.
- Keep specification artifacts under `.specs/<NNN>-<feature>/`.
